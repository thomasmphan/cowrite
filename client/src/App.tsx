import { ReactNode } from 'react';

export default function App(): ReactNode {
  return (
    <div className="flex items-center justify-center min-h-screen bg-gray-100">
      <h1 className="text-3x1 font-bold text-gray-900">CoWrite</h1>
    </div>
  );
}
