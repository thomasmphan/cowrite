{
  "name": "cowrite",
  "private": true,
  "workspaces": [
    "packages/*",
    "server",
    "client"
  ],
  "scripts": {
    "dev:server": "npm run dev -w server",
    "build": "npm run build -w @cowrite/shared && npm run build -w server",
    "test": "npm run test -w server",
    "test:server": "npm run test -w server"
  },
  "engines": {
    "node": ">=20"
  }
}
